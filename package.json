{
	"scripts": {
		"build": "pnpm run -r build",
		"prepublish": "pnpm run -r build && changeset",
		"version": "changeset version",
		"publish": "changeset publish"
	},
	"keywords": [],
	"author": "LAlex <lalex@lalex.com>",
	"license": "MIT",
	"devDependencies": {
		"@changesets/cli": "^2.26.0",
		"@rspack/cli": "^0.1.0",
		"@rspack/plugin-node-polyfill": "^0.1.0",
		"@types/node": "16",
		"concurrently": "^7.6.0",
		"typescript": "^4.9.5"
	}
}
